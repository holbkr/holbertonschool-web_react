# 3. Update the remained styles

Convert the `Header`, `Login`, `BodySection`, `BodySectionWithMarginBottom` and `Footer` components' styling from CSS to TailwindCSS utility classes.

## Files

- `dashboard/src/Header/Header.jsx`: flexbox layout, heading colored with `--main-color`
- `dashboard/src/Login/Login.jsx`: flexbox form layout, top border colored with `--main-color`
- `dashboard/src/BodySection/BodySection.jsx` and `BodySectionWithMarginBottom.jsx`: spacing and typography
- `dashboard/src/Footer/Footer.jsx`: top border colored with `--main-color`, pinned to the bottom of the layout
