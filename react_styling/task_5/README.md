# 5. Animation

Apply a TailwindCSS bounce animation to the "Your notifications" title.

## Files

- `dashboard/src/Notifications/Notifications.jsx`: conditionally applies the `animate-bounce` class whenever `notifications.length > 0` and `displayDrawer` is `false`
